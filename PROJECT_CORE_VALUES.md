[KECLC 프로젝트 전역 아키텍처 및 AI 코딩 절대 원칙]

너는 지금부터 KECLC(KEC Calculation Suite) 프로젝트의 프론트엔드 개발을 보조한다. 
이 프로젝트는 단순한 웹페이지가 아니라, 다중 탭 간 0.1초의 지연도 없이 상태가 동기화되어야 하는 실시간 계산 시스템이다. 
코드를 수정하거나 작성할 때 아래의 원칙을 단 하나라도 어기면 치명적인 버그로 간주한다.

1. Memory-First & Optimistic Update (가장 중요)
- 모든 UI는 서버(DB)가 아닌 Zustand 전역 스토어를 최우선으로 구독(Subscribe)하여 렌더링한다.
- 데이터 변경 이벤트(추가/수정/연결/해제 등) 발생 시, API 응답을 절대 기다리지 마라. 즉시 useDataStore.getState()... 등을 통해 로컬 메모리(Zustand) 상태부터 강제로 업데이트(Optimistic Update)해라.
- 서버에서 내려온 임시(Draft) 데이터가 로컬의 최신 메모리 스토어를 덮어쓰거나 역류하는 일은 결코 없어야 한다. 데이터의 흐름은 철저히 로컬에서 서버로 향한다.

2. 실시간 동기화와 인디케이터 트리거 (The Blue/Green Rule)
- 로컬 메모리를 갱신했다면, 해당 변경 사항이 서버에 디바운스(Debounce) 저장되고 타 탭에 전파되도록 반드시 동기화 스위치를 켜야 한다.
- 사용자의 조작(UI 클릭, 입력 등)이 발생한 핸들러 내부에서는 반드시 **isLocalChangeRef.current = true**를 선언하여, 시스템 인디케이터가 Yellow(Saving) -> Blue(Syncing) -> Green(Saved)으로 정상 작동하며 브로드캐스트가 발신되도록 만들어라.

3. Zero-Sync 브로드캐스팅 (BroadcastChannel)
- 상태가 변경되거나 연결이 해제(Falsy 값 포함)되었을 때, 변경된 페이로드를 즉시 타 탭으로 브로드캐스트(UPDATE_PANEL 등)해라.
- 타 탭의 신호를 수신하는 부분에서는 상태 덮어쓰기(Race Condition)를 막기 위해 반드시 함수형 업데이트(prev => ...)를 사용하고, 로컬 상태(useState)를 섞어 쓰지 마라.

4. 작업 진행 및 검증 원칙
- 한 번에 여러 개의 계산서 모듈이나 로직을 동시다발적으로 건드리지 마라.
- 반드시 하나의 계산서 시트(또는 단일 기능) 작업을 완료한 후, 콘솔 에러가 없는지 사용자의 확인과 검증을 받은 뒤에만 다음 항목으로 넘어가라.
