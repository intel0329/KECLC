# KECLC 개발자 모드 & 브로드캐스트 레이더 (Broadcast Radar) 가이드

이 문서는 KECLC(KEC Calculation Suite) 시스템의 탭 간 실시간 동기화 상태를 추적하고, 패킷 흐름을 시각적으로 디버깅할 수 있도록 설계된 **개발자 전용 모니터링 레이더**의 조작법과 핵심 기술 용어를 정리한 문서입니다.

---

## 1. 개요 및 활성화 방법

* **개발자 모드 활성화:** 화면 아무 곳이나 포커스된 상태에서 **`Ctrl + Shift + D`** 단축키를 입력합니다.
* **레이더 모니터링 창 노출:** 
  단축키를 입력하면 브라우저 우측 하단에 반투명한 다크 글래스모피즘 (`backdrop-blur-md bg-black/75 border border-white/10`) 스타일의 터미널 감성 창이 활성화됩니다.
* **로컬 캐시 반영:** 활성화 상태는 브라우저 로컬 저장소(`localStorage`의 `KECLC_DEV_MODE: 'true'/'false'`)에 즉시 기록되므로, 페이지를 새로고침하거나 창을 넘어가도 상태가 지속적으로 유지됩니다.

---

## 2. 브로드캐스트 레이더 용어 해설

레이더 화면에 로깅되는 각 항목과 변수들은 실시간 크로스 탭 동기화 채널인 `BroadcastChannel (KECLC_STATE_SYNC)`을 통해 유통되는 통신 패킷의 상세 스냅샷입니다.

### 2.1 패킷 방향 및 상태 (Type)

로그의 가장 좌측에 대괄호(`[...]`)로 감싸여 표시되며, 용도에 따라 세련된 터미널 전용 색상 코드로 차별화되어 출력됩니다.

| 표시 유형 | 색상 코드 | 풀네임 (Full name) | 상세 설명 |
| :--- | :--- | :--- | :--- |
| **`[TX]`** | **시안 블루**<br>`text-sky-400` | Transmit (송신) | **현재 사용자가 직접 조작하고 있는 탭(Local Tab)**에서 데이터 변경이 일어나, 브로드캐스트 채널을 통해 다른 모든 브라우저 탭들로 동기화 신호를 방출한 상태입니다. |
| **`[RX]`** | **라벤더 퍼플**<br>`text-fuchsia-400` | Receive (수신) | **동일 브라우저 내의 다른 탭(Remote Tab)**에서 발생한 수정 신호를 현재 탭이 성공적으로 감지하여, 자신의 메모리 스토어(Zustand)에 동기화 완료한 상태입니다. |
| **`[WARN]`** | **앰버 옐로우**<br>`text-yellow-500` | Warning (경고) | 백그라운드 연산 중 주의가 필요하거나 예외(Exception) 상황이 감지된 디버깅 신호입니다. (예: 캐시 한도 초과, 서버 데이터베이스 백그라운드 업로드 실패 등) |

---

### 2.2 송신자 식별 정보 (Sid)

동기화 신호를 송출한 브라우저 탭의 고유 식별자(Sender ID)를 식별합니다.

* **`LOCAL`**: 현재 보고 있는 **본인의 탭**에서 직접 송신(`[TX]`)했음을 의미합니다.
* **`usgp`, `a7x9` 등 4자리 문자열**: 
  * 개별 브라우저 탭이 실행될 때마다 RAM에서 생성되는 무작위 고유 해시(`TAB_ID`)의 **마지막 4자리 슬라이스 값**입니다.
  * 다른 탭에서 보낸 수신(`[RX]`) 패킷일 경우, 어떤 탭으로부터 동기화 신호가 왔는지 직관적으로 역추적할 수 있게 도와줍니다.

---

### 2.3 패킷 동기화 액션 (Action)

탭 간 동기화를 전파하는 비즈니스 메시지의 종류와 목적입니다.

* **`UPDATE_PANEL`**: 
  * 분전반, 동력부하, 변압기 등 특정 계산서 시트의 입력값(회로 스펙, 전선 종류, 부하값 등)이 변경되어 메모리 및 디렉토리 구조가 실시간 교환되고 있음을 알리는 가장 대표적인 패킷입니다.
* **`UPDATE_RESULTS`**: 
  * KEC 연산 엔진이 즉시 계산해 낸 최종 합산 부하 및 차단기 판정 결과물(Zustand 스토어의 `results` 맵)을 다른 탭으로 실시간 전파하는 패킷입니다.
* **`UPDATE_SETTINGS`**: 
  * KEC 법규 기준치, TCC 배율, 모터 시트 계수 등 시스템 전체에 공통 적용되는 기본 설정이 탭 간 공유될 때 발생합니다.
* **`CLOSE_PROJECT`**: 
  * 한 탭에서 사용자가 프로젝트를 완전히 닫아 홈으로 나갔을 때, 열려 있는 다른 모든 탭들도 동시에 홈 화면이나 프로젝트 선택창으로 함께 즉각 동기화 리다이렉트 시켜주는 세션 관리 전파 메시지입니다.
* **`PROJECT_DELETED` / `REQ_ACTIVE_PROJECT`**: 
  * 특정 프로젝트가 삭제되었거나 타 탭의 활성 세션을 탐색하기 위해 주고받는 관리용 패킷입니다.

---

## 3. 브로드캐스트 레이더의 3대 안전 설계 규칙

개발자 모드를 유지보수하거나 기능을 확장할 때는 시스템 아키텍처의 무결성을 보장하기 위해 아래 수칙을 반드시 준수해야 합니다.

1. **비침습성 유지 (Read-Only Guard)**
   * 브로드캐스트 레이더는 데이터의 흐름을 **오직 관찰하고 읽는 역할(Read-Only)**만 수행합니다.
   * 로그를 렌더링하기 위해 Zustand 스토어에 추가적인 쓰기(`set`) 행위를 절대 발생시키지 않으며, 전역 렌더링 루프에 어떠한 사이드 이펙트도 주지 않도록 순수 CustomEvent 통로로만 작동합니다.
2. **동기화 회귀 방어막 (Regression Safeguard)**
   * `useDataStore.js` 내부의 **지문 기반 노이즈 필터 가드(`lastSyncFingerprintRef`)**와 **동적 트랜잭션 참조 가드(`lastFromIdRef`)**는 1mm도 수정되거나 제거되어서는 안 됩니다. 이 필터들은 탭 간 무한 핑퐁 루프를 막아주는 마지막 안전장치입니다.
3. **용량 및 자원 관리 (Memory Leak Prevention)**
   * 브로드캐스트 레이더의 로그 배열은 항상 **최대 10개**로 큐 버퍼 크기가 단단히 고정되어 있습니다. 탭을 무한히 열어두어도 메모리 점유율이 누적 증가하지 않고 가볍고 민첩하게 유지되도록 설계되어 있습니다.
